{
  "name": "tsnl",
  "version": "0.0.2",
  "author": "forsigner @forsigner",
  "bin": {
    "tsnl": "./bin/run"
  },
  "bugs": "https://github.com/forsigner/tsnl/issues",
  "dependencies": {
    "@oclif/command": "^1.5.19",
    "@oclif/config": "^1.13.3",
    "@oclif/plugin-help": "^2.2.1",
    "@types/jest": "^24.0.18",
    "cross-spawn": "^7.0.0",
    "eslint-config-leaf-app": "0.0.3",
    "fs-extra": "^8.1.0",
    "jest": "^24.9.0",
    "prettier": "^1.18.2",
    "ts-jest": "^24.0.2",
    "tslib": "^1.10.0"
  },
  "devDependencies": {
    "@oclif/dev-cli": "^1.22.2",
    "@types/cross-spawn": "^6.0.0",
    "@types/fs-extra": "^8.0.0",
    "@types/node": "^12.7.4",
    "globby": "^10.0.1",
    "standard-version": "^7.0.0",
    "ts-node": "^8.3.0",
    "typescript": "^3.6.2"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "files": [
    "/bin",
    "/lib",
    "/template",
    "/npm-shrinkwrap.json",
    "/oclif.manifest.json"
  ],
  "homepage": "https://github.com/forsigner/tsnl",
  "keywords": [
    "oclif"
  ],
  "license": "MIT",
  "main": "lib/index.js",
  "oclif": {
    "commands": "./lib/commands",
    "bin": "tsnl",
    "plugins": [
      "@oclif/plugin-help"
    ]
  },
  "repository": "forsigner/tsnl",
  "scripts": {
    "postpack": "rm -f oclif.manifest.json",
    "prepack": "rm -rf lib && tsc -b && oclif-dev manifest && oclif-dev readme",
    "build": "rm -rf lib && tsc -b && oclif-dev manifest && oclif-dev readme",
    "test": "echo NO TESTS",
    "version": "oclif-dev readme && git add README.md",
    "release": "standard-version"
  },
  "types": "lib/index.d.ts"
}
